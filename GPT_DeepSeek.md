# GPT-1 & DeepSeek-R1 논문 리뷰

## 1. GPT-1

### 1.1 논문 정보

- **논문명:** Improving Language Understanding by Generative Pre-Training
- **저자:** Alec Radford, Karthik Narasimhan, Tim Salimans, Ilya Sutskever
- **발표 연도:** 2018
- **논문 링크:** https://cdn.openai.com/research-covers/language-unsupervised/language_understanding_paper.pdf
- **핵심 키워드:** Generative Pre-training, Fine-tuning, Transformer Decoder, Transfer Learning

> 논문에서는 모델을 GPT-1이라고 직접 부르지 않고 `Finetuned Transformer LM`이라고 표현함. 이후 등장한 GPT 계열 모델과 구분하기 위해 일반적으로 GPT-1이라고 부름.

---

### 1.2 연구 배경

기존 자연어 처리 모델은 감성 분석, 질의응답, 문장 관계 판단 등 각각의 과제를 학습하기 위해 많은 라벨 데이터가 필요했음. 그러나 사람이 직접 정답을 붙인 데이터는 수집 비용이 크고, 특정 분야에서는 충분한 데이터를 구하기 어려움.

반면 책과 웹 문서처럼 라벨이 없는 텍스트는 매우 많음. GPT-1은 이러한 비정형 텍스트로 먼저 언어를 학습한 뒤, 과제별로 적은 양의 라벨 데이터를 이용해 Fine-tuning하는 방법을 제안함.

---

### 1.3 핵심 아이디어

GPT-1은 다음의 2단계 학습 방식을 사용함.

```text
1단계: Unsupervised Pre-training
라벨이 없는 대규모 텍스트로 다음 단어 예측 학습

2단계: Supervised Fine-tuning
과제별 라벨 데이터로 모델 전체를 추가 학습
```

핵심 가정은 다음과 같음.

> 다음 단어를 정확하게 예측하려면 단어의 의미, 문법, 문장 구조, 문맥과 상식 등을 함께 학습해야 하므로, 언어 모델링을 통해 배운 지식을 다른 자연어 처리 과제에도 활용할 수 있음.

---

### 1.4 모델 구조

GPT-1은 Transformer의 Decoder 부분만 사용한 **Decoder-only Transformer**임.

| 항목 | GPT-1 |
|---|---:|
| Transformer Layer | 12개 |
| Hidden Size | 768 |
| Attention Head | 12개 |
| 최대 입력 길이 | 512토큰 |
| 토크나이저 | BPE |
| 파라미터 수 | 약 117M |
| 위치 표현 | 학습 가능한 Position Embedding |

GPT-1은 Masked Self-Attention을 사용하기 때문에 현재 토큰을 예측할 때 오른쪽에 있는 미래 토큰을 볼 수 없음.

```text
입력: 나는 오늘 학교에
예측: 갔다
```

이러한 방식으로 이전 토큰들을 참고하여 다음 토큰을 순차적으로 예측함.

---

### 1.5 Unsupervised Pre-training

GPT-1은 약 7,000권 이상의 미출간 도서로 구성된 BooksCorpus를 이용해 사전학습함. BooksCorpus는 문장이 임의로 섞여 있지 않고 긴 내용이 연결되어 있어 장거리 문맥을 학습하는 데 적합함.

사전학습 목표는 다음과 같음.

$$
L_1(U)=\sum_i \log P(u_i \mid u_{i-k},\dots,u_{i-1})
$$

앞에 나온 토큰을 이용해 실제 다음 토큰이 등장할 확률을 높이는 방식임.

다음 단어 예측을 반복하면서 모델은 다음 정보를 자연스럽게 학습함.

- 단어의 의미와 사용 방식
- 문법과 문장 구조
- 앞뒤 문장의 관계
- 장거리 문맥
- 텍스트에 포함된 일부 상식

---

### 1.6 Supervised Fine-tuning

사전학습이 끝난 GPT-1에 과제별 출력층을 추가하고, 라벨 데이터로 모델 전체를 Fine-tuning함.

```text
입력 문장
→ 사전학습된 GPT-1
→ 과제별 Linear Layer
→ 분류 결과
```

이때 출력층만 학습하는 것이 아니라 GPT-1 내부 파라미터도 함께 조정함. 따라서 GPT-1은 사전학습 모델을 고정해서 특징만 추출하는 Feature-based 방식이 아니라 **Fine-tuning 방식**을 사용함.

논문에서는 과제별 목적함수와 언어 모델링 목적함수를 함께 사용하는 방법도 제안함.

$$
L_3(C)=L_2(C)+\lambda L_1(C)
$$

- $L_2$: 과제의 정답을 예측하는 목적
- $L_1$: 다음 단어를 예측하는 언어 모델링 목적
- $\lambda$: 두 목적의 비중

보조 언어 모델링 목적은 일부 큰 데이터셋의 일반화에 도움이 되었지만, 모든 과제의 성능을 높인 것은 아님.

---

### 1.7 Task-specific Input Transformation

GPT-1은 과제마다 복잡한 전용 모델을 새로 설계하는 대신, 다양한 형태의 입력을 하나의 토큰 시퀀스로 변환해 같은 Transformer에 입력함.

#### 문장 분류

```text
[Start] 문장 [Extract]
```

마지막 `[Extract]` 토큰의 표현을 이용해 감성이나 문법성을 분류함.

#### 자연어 추론

```text
[Start] 전제문장 [Delimiter] 가설문장 [Extract]
```

두 문장이 함의, 모순, 중립 중 어떤 관계인지 판단함.

#### 문장 유사도

두 문장의 순서가 결과에 영향을 주지 않도록 두 가지 순서를 모두 입력함.

```text
문장 A [Delimiter] 문장 B
문장 B [Delimiter] 문장 A
```

두 결과를 합쳐 문장 유사도를 판단함.

#### 객관식 질의응답

```text
지문 + 질문 + 보기 1
지문 + 질문 + 보기 2
지문 + 질문 + 보기 3
```

각 보기와 결합한 입력을 독립적으로 처리한 뒤 가장 적절한 답을 선택함.

현재의 ChatGPT처럼 답을 자유롭게 생성하는 방식이 아니라 주어진 보기 중 정답을 선택하는 방식임.

---

### 1.8 실험 결과

GPT-1은 다음 네 종류의 자연어 이해 과제를 평가함.

- 자연어 추론
- 질의응답 및 상식 추론
- 문장 유사도
- 문장 분류

총 12개 데이터셋 중 9개에서 당시 최고 성능을 기록함.

| 과제 | 기존 최고 성능 | GPT-1 | 향상 |
|---|---:|---:|---:|
| Story Cloze | 77.6 | 86.5 | +8.9%p |
| RACE | 53.3 | 59.0 | +5.7%p |
| SciTail | 83.3 | 88.3 | +5.0%p |
| QNLI | 82.3 | 88.1 | +5.8%p |
| GLUE | 68.9 | 72.8 | +3.9 |

하지만 학습 데이터가 적은 RTE에서는 기존 Multi-task BiLSTM 모델보다 낮은 성능을 보임. 따라서 사전학습이 모든 과제에서 항상 최고 성능을 보장하는 것은 아님.

---

### 1.9 Ablation Study

#### 사전학습의 효과

| 모델 | 평균 점수 |
|---|---:|
| Transformer with Pre-training | 74.7 |
| Transformer without Pre-training | 59.9 |

사전학습을 제거했을 때 평균 점수가 14.8점 감소함. 이를 통해 성능 향상의 핵심이 Transformer 구조뿐만 아니라 대규모 사전학습에 있음을 확인함.

#### Transformer와 LSTM 비교

| 모델 | 평균 점수 |
|---|---:|
| Transformer | 74.7 |
| LSTM | 69.1 |

LSTM으로 교체했을 때 평균 점수가 5.6점 감소함. Transformer의 Self-Attention 구조가 긴 문맥을 학습하고 다른 과제로 지식을 전이하는 데 더 적합했음을 보여줌.

---

### 1.10 논문의 의의

GPT-1은 다음의 학습 패러다임을 효과적으로 보여준 초기 연구임.

```text
대규모 텍스트 사전학습
→ 과제별 Fine-tuning
→ 다양한 NLP 과제에 재사용
```

이전에는 사전학습된 단어 임베딩을 특징으로 활용하는 방식이 일반적이었지만, GPT-1은 Transformer 전체를 사전학습한 뒤 과제별로 Fine-tuning하는 방법의 가능성을 보여줌.

이러한 방식은 이후 BERT, GPT-2, GPT-3 등 대규모 사전학습 언어 모델 발전의 기반이 됨.

---

### 1.11 한계

- 과제마다 라벨 데이터와 별도의 Fine-tuning이 필요함.
- 현재 GPT처럼 Prompt만으로 여러 과제를 수행하는 모델이 아님.
- 최대 입력 길이가 512토큰으로 제한됨.
- BooksCorpus라는 한 종류의 데이터에 크게 의존함.
- 작은 데이터셋에서는 충분한 성능 향상이 나타나지 않음.
- 논문의 Zero-shot 실험은 제한적인 휴리스틱 방식으로, 현재 LLM의 Zero-shot 사용 방식과 차이가 있음.

---

### 1.12 핵심 정리

> GPT-1은 Decoder-only Transformer를 라벨 없는 텍스트로 사전학습한 뒤, 과제별 데이터로 Fine-tuning하면 하나의 모델을 다양한 자연어 이해 과제에 재사용할 수 있음을 보여준 모델임.

---

<br>

# 2. DeepSeek-R1

### 2.1 논문 정보

- **논문명:** DeepSeek-R1: Incentivizing Reasoning Capability in LLMs via Reinforcement Learning
- **저자:** DeepSeek-AI
- **논문 버전:** arXiv v2, 2026
- **논문 링크:** https://arxiv.org/abs/2501.12948
- **핵심 키워드:** Reinforcement Learning, GRPO, Chain-of-Thought, Reasoning Model, Distillation

---

### 2.2 연구 배경

기존 LLM의 추론 능력을 높이기 위해서는 사람이 작성한 고품질 Chain-of-Thought 풀이를 이용해 Supervised Fine-tuning하는 방법이 주로 사용됨.

그러나 사람이 복잡한 추론 과정을 직접 작성하는 것은 비용이 크고 확장하기 어려움. 또한 인간의 풀이 방식을 그대로 따라 하게 하면 모델이 스스로 더 효과적인 추론 전략을 탐색하는 것을 제한할 가능성이 있음.

DeepSeek-R1 논문은 다음 질문에서 출발함.

> 사람이 풀이 과정을 직접 가르치지 않고 최종 답의 정확성만 보상해도 모델이 스스로 추론 전략을 발달시킬 수 있는가?

---

### 2.3 기반 모델

DeepSeek-R1은 처음부터 사전학습한 모델이 아니라 이미 학습된 **DeepSeek-V3-Base**를 기반으로 함.

| 항목 | DeepSeek-V3-Base |
|---|---:|
| 구조 | Mixture-of-Experts |
| 전체 파라미터 | 671B |
| 토큰당 활성 파라미터 | 37B |
| 사전학습 데이터 | 14.8조 토큰 |
| 주요 학습 언어 | 중국어·영어 |

Mixture-of-Experts는 모든 파라미터를 매번 사용하는 것이 아니라, 각 토큰을 처리하는 데 필요한 일부 Expert만 선택적으로 활성화하는 구조임.

따라서 DeepSeek-R1 논문의 핵심은 새로운 모델 아키텍처보다 **사전학습된 모델에 어떤 Post-training 방법을 적용했는가**에 있음.

---

### 2.4 DeepSeek-R1-Zero와 DeepSeek-R1

논문에서는 두 모델을 구분해야 함.

| 모델 | 학습 방식 | 특징 |
|---|---|---|
| DeepSeek-R1-Zero | SFT 없이 바로 RL 적용 | 순수 RL의 가능성을 실험 |
| DeepSeek-R1 | Cold Start SFT, RL, 800K SFT, 2차 RL | 성능과 가독성을 개선한 최종 모델 |

최종 DeepSeek-R1은 SFT와 RL을 함께 사용하므로 순수 강화학습 모델이 아님. **SFT 없이 강화학습을 시작한 모델은 R1-Zero임.**

---

## 2.5 DeepSeek-R1-Zero

### 학습 방법

기존 LLM의 일반적인 Post-training 방식은 다음과 같음.

```text
사전학습 모델
→ 사람이 작성한 모범 답안으로 SFT
→ 인간 선호도 기반 RL
```

R1-Zero는 SFT를 생략하고 DeepSeek-V3-Base에 바로 강화학습을 적용함.

```text
DeepSeek-V3-Base
→ GRPO 기반 강화학습
→ DeepSeek-R1-Zero
```

사람이 구체적인 풀이 방법을 알려 주지 않고, 모델이 여러 풀이를 생성한 뒤 최종 답과 형식만 평가함.

---

### 2.6 Reward Design

R1-Zero의 보상은 다음 두 요소로 구성됨.

$$
Reward_{rule}=Reward_{accuracy}+Reward_{format}
$$

#### Accuracy Reward

- 수학 문제: 최종 답이 정답과 일치하는지 검사함.
- 코딩 문제: 생성한 코드가 테스트 케이스를 통과하는지 검사함.
- 객관식 문제: 올바른 보기를 선택했는지 검사함.

#### Format Reward

모델이 추론 과정과 최종 답을 지정된 태그 안에 작성했는지 검사함.

```text
<think>추론 과정</think>
<answer>최종 답</answer>
```

추론 과정의 각 단계를 사람이 평가하거나 정답 풀이와 비교하지 않고, **최종 답과 출력 형식만 평가함.**

---

### 2.7 GRPO

DeepSeek-R1은 Group Relative Policy Optimization을 강화학습 알고리즘으로 사용함.

한 문제에 대해 여러 개의 답을 생성하고 각 답의 보상을 같은 그룹 안에서 비교함.

```text
하나의 문제
→ 16개의 답안 생성
→ 각 답안의 점수 계산
→ 그룹 평균보다 좋은 답안은 확률 증가
→ 그룹 평균보다 나쁜 답안은 확률 감소
```

각 답안의 Advantage는 다음과 같이 계산함.

$$
A_i=\frac{r_i-\operatorname{mean}(r_1,\dots,r_G)}
{\operatorname{std}(r_1,\dots,r_G)}
$$

#### PPO와 GRPO 비교

| PPO | GRPO |
|---|---|
| 별도의 Value Model이 필요함 | Value Model이 필요하지 않음 |
| 중간 상태에서 최종 보상을 예측함 | 같은 문제에서 생성된 답안들을 비교함 |
| 메모리와 연산 비용이 큼 | 상대적으로 효율적임 |
| 긴 추론에서 Value 예측이 어려움 | 결과 중심의 긴 추론 학습에 적합함 |

긴 추론에서는 처음에 잘못된 방법을 선택했다가 이후 이를 수정할 수 있음. 따라서 생성 중간 단계만 보고 최종 보상을 예측하는 Value Model을 학습하기 어려움.

GRPO는 별도의 Value Model 없이 같은 문제에서 생성된 답안들의 상대적 보상을 이용함. 또한 KL 페널티를 사용하여 학습 중인 모델이 기준 모델에서 지나치게 멀어지지 않도록 제한함.

---

### 2.8 추론 행동의 발달

R1-Zero는 학습이 진행될수록 다음과 같은 추론 행동을 더 많이 생성함.

- 풀이 과정 검증
- 계산 오류 발견
- 앞 단계로 되돌아가기
- 다른 풀이 방법 탐색
- 최종 답 재확인
- 어려운 문제에서 더 긴 추론 생성

논문의 Figure 9에서는 `wait`, `mistake`, `verify`, `check` 등 반성적인 단어의 등장 빈도가 학습 초반보다 약 5~7배 증가함.

논문은 모델이 풀이 도중 오류를 발견하고 다시 계산하는 사례를 `Aha Moment`라고 표현함.

다만 이는 모델이 인간처럼 의식적으로 깨달았다는 의미가 아님. 관찰된 것은 오류 수정과 자기검증 형태의 텍스트가 증가했다는 것이며, 출력된 Chain-of-Thought가 모델의 실제 내부 사고를 완전히 설명한다고 보기는 어려움.

---

### 2.9 R1-Zero 성능

AIME 2024에서 강화학습이 진행됨에 따라 다음과 같이 성능이 향상됨.

| 모델 및 평가 방식 | AIME 2024 |
|---|---:|
| 강화학습 전 | 15.6% |
| R1-Zero Pass@1 | 77.9% |
| R1-Zero Cons@16 | 86.7% |

- **Pass@1:** 하나의 답을 생성했을 때의 평균 정답률
- **Cons@16:** 답을 16개 생성한 뒤 가장 많이 등장한 최종 답을 선택한 결과

학습이 진행될수록 모델의 평균 응답 길이도 증가함. 다만 8,200 Step 이후 최대 출력 길이를 32,768토큰에서 65,536토큰으로 늘렸기 때문에 해당 시점의 길이 증가는 학습 효과와 출력 제한 변경의 영향을 함께 받음.

---

### 2.10 R1-Zero의 문제점

R1-Zero는 높은 추론 성능을 보였지만 다음 문제가 나타남.

- 영어와 중국어가 섞이는 Language Mixing
- 지나치게 길고 반복적인 추론
- 읽기 어려운 문장과 형식
- 일반 글쓰기와 오픈 도메인 질의응답 성능 부족
- 사용자의 지시를 정확하게 따르는 능력 부족

이를 해결하기 위해 R1-Zero의 추론 능력에 SFT와 추가 RL을 결합하여 최종 DeepSeek-R1을 개발함.

---

## 2.11 최종 DeepSeek-R1 학습 과정

최종 R1은 다음과 같은 다단계 파이프라인으로 학습함.

```text
R1-Zero의 추론 결과 수집
→ Cold Start SFT
→ 1차 강화학습
→ Rejection Sampling
→ 약 800K 데이터로 2차 SFT
→ 2차 강화학습
→ 최종 DeepSeek-R1
```

### 1단계: Cold Start SFT

R1-Zero가 생성한 추론 결과 중 다음 조건을 만족하는 결과를 선택함.

- 최종 답이 정확함.
- 출력 형식이 읽기 쉬움.
- 언어가 섞이지 않음.
- 불필요한 반복이 없음.

이후 DeepSeek-V3와 사람이 추론 표현을 다듬고 검수함. 이를 통해 R1-Zero의 추론 능력을 유지하면서 사람이 읽기 쉬운 답변을 생성하도록 Fine-tuning함.

### 2단계: 1차 강화학습

Cold Start 모델에 다시 GRPO를 적용함. 이 단계에서는 다음 보상을 사용함.

- 정답 보상
- 형식 보상
- 언어 일관성 보상

언어 일관성 보상은 질문과 같은 언어로 추론과 답을 작성하도록 유도함. 가독성은 향상되었지만 일부 코딩 성능은 소폭 하락하여 사용자 선호와 과제 성능 사이의 Trade-off가 나타남.

### 3단계: Rejection Sampling 및 800K SFT

1차 RL 모델이 여러 답을 생성하게 한 뒤 정답과 형식을 통과한 결과만 남김.

```text
여러 풀이 생성
→ 정답과 형식 검사
→ 좋은 풀이만 선택
→ SFT 데이터로 재사용
```

최종적으로 약 80만 개의 데이터를 수집함.

| 데이터 | 규모 |
|---|---:|
| 추론 데이터 | 약 600K |
| 비추론 데이터 | 약 200K |
| 전체 | 804,745개 |

비추론 데이터에는 글쓰기, 번역, 일반 질의응답, 프로그램 수정, 웹 개발 등이 포함됨. 이를 통해 수학과 코딩뿐 아니라 일반적인 답변 능력도 향상시킴.

### 4단계: 2차 강화학습

마지막 단계에서는 추론 능력과 인간 선호도를 함께 최적화함.

- 수학·코딩·논리 문제: 규칙 기반 정답 보상
- 일반 질문: Helpfulness Reward Model
- 안전 관련 질문: Safety Reward Model
- 전체 질문: 언어 일관성 보상

이를 통해 추론 능력, 지시 수행, 도움됨과 안전성을 함께 개선한 최종 DeepSeek-R1을 완성함.

---

### 2.12 실험 결과

첨부된 v2 논문의 Table 8 기준 결과임.

| Benchmark | GPT-4o | OpenAI o1-1217 | DeepSeek-R1 |
|---|---:|---:|---:|
| AIME 2024 | 9.3 | 79.2 | **79.8** |
| MATH-500 | 74.6 | 96.4 | **97.3** |
| LiveCodeBench | 32.9 | 63.4 | **65.9** |
| Codeforces Rating | 759 | **2,061** | 2,029 |
| GPQA Diamond | 49.9 | **75.7** | 71.5 |
| MMLU-Pro | 72.6 | - | **84.0** |
| SimpleQA | 38.2 | **47.0** | 30.1 |
| SWE-Bench Verified | 38.8 | 48.9 | **49.2** |

DeepSeek-R1은 수학과 알고리즘 코딩에서 특히 높은 성능을 보임. AIME 2024와 MATH-500에서는 OpenAI o1-1217과 비슷하거나 조금 높은 성능을 기록함.

반면 GPQA, Codeforces Rating, SimpleQA 등에서는 o1-1217보다 낮은 성능을 보임. 따라서 DeepSeek-R1이 모든 과제에서 기존 모델보다 우수하다고 해석할 수는 없음.

---

### 2.13 Test-time Compute Scaling

DeepSeek-R1은 문제의 난이도에 따라 사용하는 추론 토큰의 수를 조절함.

```text
쉬운 문제
→ 짧은 추론

어려운 문제
→ 긴 검증, 되돌아가기, 대안 탐색
```

논문의 2024년 수학 대회 문제 실험에서 DeepSeek-R1은 문제당 평균 약 8,793개의 추론 토큰을 사용해 61.8%의 Pass@1을 기록함. 가장 어려운 문제에서는 18,000개 이상의 추론 토큰을 사용함.

이처럼 학습된 파라미터를 변경하지 않고 추론 시점에 더 많은 연산을 사용해 성능을 높이는 방식을 **Test-time Compute Scaling**이라고 함.

다만 추론이 길어져도 잘못된 논리에 갇히거나 쉬운 질문을 지나치게 복잡하게 생각하는 Overthinking 문제가 발생할 수 있음.

---

### 2.14 Distillation

DeepSeek-R1은 671B 규모로 실행 비용이 크기 때문에, R1이 생성한 고품질 추론 데이터를 더 작은 Qwen과 Llama 모델에 학습시킴.

```text
DeepSeek-R1이 추론 데이터 생성
→ Qwen·Llama 기반 모델에 SFT
→ 작은 추론 모델 생성
```

`DeepSeek-R1-Distill-Qwen`은 R1의 파라미터를 단순히 축소한 모델이 아니라, **Qwen 기반 모델이 R1의 생성 결과를 학습한 모델**임.

| 모델 | AIME 2024 | MATH-500 | LiveCodeBench |
|---|---:|---:|---:|
| R1-Distill-Qwen-1.5B | 28.9 | 83.9 | 16.9 |
| R1-Distill-Qwen-7B | 55.5 | 92.8 | 37.6 |
| R1-Distill-Qwen-32B | 72.6 | 94.3 | 57.2 |
| R1-Distill-Llama-70B | 70.0 | 94.5 | 57.5 |

32B 모델에 직접 대규모 RL을 적용한 모델보다, 강한 R1의 추론 결과를 Distillation한 모델이 모든 비교 벤치마크에서 더 높은 성능을 보임.

이를 통해 작은 모델에서는 직접 대규모 강화학습을 적용하는 것보다 강한 Teacher Model의 추론 데이터를 학습하는 것이 더 효율적일 수 있음을 확인함.

---

### 2.15 논문의 의의

DeepSeek-R1 논문은 다음 사실을 실험적으로 보여줌.

- 사람이 작성한 풀이 과정 없이도 결과 중심 강화학습으로 추론 행동을 발달시킬 수 있음.
- 최종 정답을 검증할 수 있는 문제에서 규칙 기반 Reward가 효과적으로 작동함.
- RL 과정에서 자기검증, 오류 수정, 되돌아가기와 같은 행동이 증가함.
- 문제 난이도에 따라 추론 시간을 조절하는 Test-time Scaling이 가능함.
- 대형 모델의 추론 데이터를 Distillation하면 작은 모델의 성능도 크게 향상될 수 있음.
- 최종적으로는 RL과 SFT가 서로 다른 역할을 하므로 두 방법을 함께 사용하는 것이 중요함.

---

### 2.16 한계

#### Pure RL의 범위

R1-Zero가 사람의 풀이 과정 없이 RL을 시작한 것은 맞지만 완전히 인간의 정보 없이 학습한 것은 아님.

- 인간이 작성한 웹 문서로 사전학습된 V3-Base에서 시작함.
- RL 학습을 위해 정답이나 검증기가 필요함.
- 최종 R1은 사람의 검수와 SFT 데이터를 사용함.

따라서 정확한 해석은 다음과 같음.

> R1-Zero는 사람이 작성한 추론 과정으로 SFT하지 않고, 검증 가능한 최종 결과의 보상만으로 추론 행동을 발달시킴.

#### Reward Hacking

모델이 실제로 좋은 답을 생성하는 대신 Reward Model의 약점을 이용해 높은 점수를 얻는 현상이 나타날 수 있음. 논문에서도 Reward 점수는 증가하지만 실제 Codeforces 성능은 하락하는 사례를 보고함.

#### 검증 가능한 문제에 의존

수학 정답이나 코드 테스트처럼 결과를 자동으로 검사할 수 있는 문제에는 적용하기 쉬움. 반면 글쓰기와 창의적 답변처럼 정답이 하나가 아닌 문제에는 신뢰할 수 있는 보상을 설계하기 어려움.

#### 기타 한계

- 쉬운 문제에서도 지나치게 긴 추론을 생성할 수 있음.
- 영어와 중국어 이외의 언어에서 Language Mixing이 발생할 수 있음.
- Few-shot Prompt가 오히려 성능을 낮추는 Prompt 민감성이 있음.
- 구조화된 출력과 Tool Use 능력이 부족함.
- 실제 소프트웨어 엔지니어링 과제의 성능 향상은 제한적임.
- 출력된 Chain-of-Thought가 실제 내부 추론을 충실히 반영한다고 보장할 수 없음.
- 데이터 중복을 제거했지만 문제의 변형이나 패러프레이즈까지 완전히 제거하지 못했을 가능성이 있음.

---

### 2.17 핵심 정리

> DeepSeek-R1은 사전학습된 대규모 언어 모델에 결과 중심 강화학습을 적용하여 자기검증, 오류 수정, 대안 탐색과 같은 추론 행동을 강화한 모델임. R1-Zero는 SFT 없이 RL을 적용한 실험 모델이며, 최종 R1은 Cold Start SFT, 두 차례의 RL과 약 800K 규모의 SFT를 결합한 모델임.

---

<br>

# 3. GPT-1과 DeepSeek-R1 비교

| 구분 | GPT-1 | DeepSeek-R1 |
|---|---|---|
| 주요 연구 질문 | 사전학습한 언어 지식을 여러 과제에 전이할 수 있는가? | RL만으로 추론 행동을 발달시킬 수 있는가? |
| 기반 구조 | 12-layer Decoder-only Transformer | 671B MoE Decoder-only LLM |
| 출발점 | 학습되지 않은 Transformer | 사전학습된 DeepSeek-V3-Base |
| 핵심 학습 | 언어 모델 사전학습 + Fine-tuning | SFT + GRPO 기반 강화학습 |
| 주요 학습 신호 | 다음 토큰과 과제 라벨 | 정답·형식·선호·안전 보상 |
| 주요 데이터 | BooksCorpus | 수학·코딩·STEM·논리·일반 질문 |
| 과제 수행 방식 | 과제마다 별도 Fine-tuning | 하나의 모델이 다양한 답변 생성 |
| 추론 특징 | 일반적인 다음 토큰 생성 | 긴 CoT, 자기검증, 되돌아가기 |
| 추론 비용 | 비교적 고정적 | 문제 난이도에 따라 증가 |
| 주요 의의 | Pre-training + Fine-tuning 패러다임 확립 | 결과 중심 RL을 통한 추론 능력 강화 |
| 주요 한계 | 과제별 라벨과 Fine-tuning 필요 | 긴 출력, Reward Hacking, 검증기 의존 |

---

# 4. 종합 결론

GPT-1과 DeepSeek-R1은 서로 다른 시기의 언어 모델 발전 방향을 보여주는 논문임.

GPT-1은 라벨 없는 대규모 텍스트로 언어 모델을 사전학습한 뒤, 과제별 Fine-tuning을 통해 하나의 모델을 여러 자연어 처리 과제에 재사용할 수 있음을 보여줌. 이는 이후 등장한 BERT와 GPT 계열 사전학습 모델의 기반이 됨.

DeepSeek-R1은 이미 대규모 사전학습을 마친 모델을 기반으로, 강화학습을 이용해 추론 전략을 발전시키는 방법을 제시함. 특히 R1-Zero를 통해 사람이 작성한 풀이 과정 없이도 정답 보상만으로 자기검증, 오류 수정, 대안 탐색과 같은 행동이 증가할 수 있음을 보여줌.

두 논문을 연결하면 다음과 같은 LLM 발전 흐름으로 정리할 수 있음.

```text
GPT-1
대규모 텍스트에서 일반적인 언어 능력 학습
→ 다양한 과제로 지식 전이

DeepSeek-V3-Base
더 큰 데이터와 모델로 언어·지식·기초 추론 능력 학습
→ 강화학습으로 추론 전략 최적화

DeepSeek-R1
긴 추론, 검증, 되돌아가기, 대안 탐색 능력 강화
```

> GPT-1이 **“언어 모델을 먼저 사전학습하고 여러 과제에 재사용하는 방법”**을 보여줬다면, DeepSeek-R1은 **“사전학습된 언어 모델이 더 오래 생각하고 스스로 풀이를 검증하도록 학습하는 방법”**을 보여준 연구임.
